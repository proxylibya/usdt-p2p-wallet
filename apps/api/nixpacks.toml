[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --include=dev || npm install"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "node scripts/start-production.js"

[variables]
NODE_ENV = "production"
NPM_CONFIG_PRODUCTION = "false"
