# 🔧 حل مشكلة الترميز العربي

## المشكلة
قاعدة البيانات PostgreSQL تستخدم ترميز WIN1252 بدلاً من UTF8، مما يمنع حفظ النصوص العربية.

## الحل السريع (اختر واحدة)

### الطريقة 1: تشغيل ملف BAT مباشرة
```cmd
cd c:\USDT-P2P-PROJECT\apps\api
FIX-DB-NOW.bat
```

### الطريقة 2: أوامر يدوية
```cmd
cd c:\USDT-P2P-PROJECT\apps\api
npx ts-node scripts/recreate-db-utf8.ts
```

### الطريقة 3: إذا فشلت الطرق السابقة
```cmd
cd c:\USDT-P2P-PROJECT\apps\api
npx prisma db push --accept-data-loss --force-reset
```

## بعد تطبيق الحل

1. شغل الخادم:
```cmd
cd c:\USDT-P2P-PROJECT\apps\api
npm run start:dev
```

2. افتح المتصفح على:
```
http://localhost:3001/admins/create
```

3. جرب إنشاء حساب أدمن بنص عربي - يجب أن يعمل الآن!

## ملاحظة مهمة
⚠️ هذا الحل سيعيد إنشاء قاعدة البيانات وسيحذف جميع البيانات الحالية.
إذا كان لديك بيانات مهمة، قم بعمل نسخة احتياطية أولاً.

## التحقق من نجاح الحل
```cmd
cd c:\USDT-P2P-PROJECT\apps\api
npx ts-node scripts/check-db-encoding.ts
```

يجب أن يظهر:
- Encoding: UTF8 ✅
