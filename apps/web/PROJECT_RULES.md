# ⚖️ قوانين وشروط المشروع الصارمة
# USDT Wallet - Strict Project Rules

> ⚠️ **هذا الملف قانون إلزامي يجب على كل مبرمج اتباعه بدون استثناء**
> 
> 📅 تاريخ الإنشاء: 2026-01-23
> 
> 🔒 **لا يجوز تعديل هذه الشروط إلا بموافقة صاحب المشروع**

---

## 🗄️ 1. قواعد البيانات (DATABASE RULES)

### ✅ مسموح (ALLOWED):
| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **PostgreSQL** | 15+ | قاعدة البيانات الرئيسية |
| **Redis** | 7+ | Cache & Sessions |
| **Prisma ORM** | 5+ | إدارة قاعدة البيانات |

### ❌ ممنوع منعاً باتاً (STRICTLY FORBIDDEN):
| التقنية | السبب |
|---------|-------|
| **SQLite** | ضعيفة وغير مناسبة للإنتاج |
| **MySQL** | PostgreSQL أفضل للمعاملات المالية |
| **MongoDB** | لا تناسب البيانات المالية المنظمة |
| **JSON Files** | غير آمنة للبيانات الحساسة |

---

## 🔌 2. الربط والاتصال (CONNECTION RULES)

### ✅ إلزامي:
- **الربط حقيقي 100%** - لا mock data في الإنتاج
- **API endpoints حقيقية** فقط
- **Backend NestJS** متصل بـ PostgreSQL
- **Prisma ORM** للتعامل مع قاعدة البيانات

### ❌ ممنوع:
- بيانات وهمية (Mock Data) في الإنتاج
- Hardcoded data في الكود
- اتصالات مباشرة بدون ORM

---

## 🛠️ 3. التقنيات المطلوبة (REQUIRED TECHNOLOGIES)

### Backend (إلزامي):
```
├── NestJS 10+           ← Framework
├── TypeScript 5+        ← Language
├── PostgreSQL 15+       ← Database
├── Prisma 5+            ← ORM
├── Redis 7+             ← Cache (اختياري للتطوير)
├── JWT                  ← Authentication
├── Passport             ← Auth Strategy
└── Swagger/OpenAPI      ← API Documentation
```

### Frontend (إلزامي):
```
├── React 19+            ← Framework
├── TypeScript 5+        ← Language
├── Vite                 ← Build Tool
├── TailwindCSS          ← Styling
├── Capacitor            ← Mobile Wrapper
└── Lucide Icons         ← Icons
```

---

## 🎨 4. قواعد التصميم (DESIGN RULES)

### ✅ إلزامي:
- **لا تغيير في التصميم الحالي** - التصميم محفوظ 100%
- **Mobile-First** - التصميم للموبايل أولاً
- **RTL Support** - دعم كامل للعربية
- **Dark Theme** - الثيم الداكن الحالي

### ❌ ممنوع:
- تغيير الألوان الأساسية بدون إذن
- إزالة أو تعديل المكونات الموجودة
- إضافة مكتبات UI جديدة بدون سبب

---

## 🔐 5. قواعد الأمان (SECURITY RULES)

### ✅ إلزامي:
- **JWT Tokens** للمصادقة
- **bcrypt** لتشفير كلمات المرور (salt rounds: 12+)
- **Rate Limiting** لحماية الـ APIs
- **Input Validation** لكل المدخلات
- **CORS** محدد للـ origins المسموحة

### ❌ ممنوع منعاً باتاً:
- تخزين كلمات المرور بدون تشفير
- API Keys في الكود المصدري
- تعطيل CORS في الإنتاج
- SQL queries مباشرة (استخدم Prisma)

---

## 📁 6. هيكل المشروع (PROJECT STRUCTURE)

### Frontend: `C:\USDT-P2P-PROJECT\apps\web\`
```
├── components/      ← UI Components
├── pages/           ← Screens
├── context/         ← React Context
├── hooks/           ← Custom Hooks
├── services/        ← API Services
├── types.ts         ← TypeScript Types
└── constants.tsx    ← Static Config Only
```

### Backend: `C:\USDT-P2P-PROJECT\apps\api\`
```
├── src/
│   ├── modules/     ← Feature Modules
│   ├── common/      ← Shared Code
│   └── main.ts      ← Entry Point
├── prisma/
│   └── schema.prisma ← Database Schema
└── .env             ← Environment Variables
```

---

## 🌐 7. البيئات والمنافذ (ENVIRONMENTS)

### Development:
| الخدمة | المنفذ | URL |
|--------|--------|-----|
| Frontend | 3001 | http://localhost:3001 |
| Backend | 3002 | http://localhost:3002 |
| PostgreSQL | 5432 | localhost:5432 |
| Redis | 6379 | localhost:6379 |
| API Docs | 3002 | http://localhost:3002/docs |

### Production:
| المتغير | القيمة |
|---------|--------|
| NODE_ENV | production |
| DATABASE_URL | PostgreSQL connection string |
| JWT_SECRET | Strong random secret (32+ chars) |

---

## 🧪 8. قواعد الاختبار (TESTING RULES)

### ✅ مطلوب:
- Unit Tests للـ Services
- Integration Tests للـ APIs
- E2E Tests للـ Critical Flows

### ❌ ممنوع:
- نشر كود بدون اختبار
- حذف tests موجودة
- تعطيل CI/CD checks

---

## 📝 9. قواعد الكود (CODE RULES)

### ✅ إلزامي:
- TypeScript strict mode
- ESLint + Prettier
- Meaningful variable names
- Comments للكود المعقد فقط

### ❌ ممنوع:
- `any` type (إلا للضرورة القصوى)
- `console.log` في الإنتاج
- Unused imports/variables
- Magic numbers بدون constants

---

## ⚠️ 10. العقوبات (VIOLATIONS)

| المخالفة | العقوبة |
|----------|---------|
| استخدام SQLite | ❌ رفض الكود فوراً |
| Mock Data في الإنتاج | ❌ رفض الكود |
| تغيير التصميم بدون إذن | ❌ إلغاء التغييرات |
| API Keys في الكود | ❌ حذف فوري + مراجعة أمنية |
| SQL Injection vulnerabilities | ❌ حظر + مراجعة كاملة |

---

## ✅ إقرار المبرمج

> أنا المبرمج أقر بأنني قرأت وفهمت جميع الشروط أعلاه وألتزم باتباعها.
> أي مخالفة لهذه الشروط قد تؤدي إلى رفض الكود أو إلغاء التغييرات.

---

## 📞 للاستفسار

في حالة الحاجة لاستثناء من أي قاعدة، يجب:
1. طلب موافقة كتابية من صاحب المشروع
2. توثيق سبب الاستثناء
3. خطة للعودة للقاعدة الأصلية

---

> 📌 **آخر تحديث:** 2026-01-23
> 
> 🔐 **هذا الملف محمي ولا يجوز تعديله بدون إذن**
